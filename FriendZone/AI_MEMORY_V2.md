# AI Memory V2

Status:
- Active canonical memory for the iOS app repo.
- Scope: `/Users/safaeralabs/Desktop/Development/FriendZone/FrontEnd-iOS/FriendZone`
- Agent: `Coder-2`
- Model: `GPT-5 Codex`
- Started: `2026-03-12 23:45:51 CET`
- Policy: append-only notes with concrete code-backed observations; do not trust `AI_MEMORY.md` as current source of truth.

Entry format:
- `timestamp` | `agent` | `model` | `scope` | `summary`

Entries:
- `2026-03-12 23:45:51 CET` | `Coder-2` | `GPT-5 Codex` | `memory-v2` | Created `AI_MEMORY_V2.md` because prior memory was stale and contradicted the current codebase. This file supersedes earlier ad hoc memory for iOS work.
- `2026-03-12 23:45:51 CET` | `Coder-2` | `GPT-5 Codex` | `repo-state` | Current iOS worktree is dirty. Modified files include `FriendZone/Core/AppSessionStore.swift`, `FriendZone/Features/Creator/NativeCreatorSpaceHubView.swift`, `FriendZone/Features/Hangouts/CreateHangoutView.swift`, `FriendZone/Features/Hangouts/HangoutDetailView.swift`, `FriendZone/Features/Hangouts/HangoutModels.swift`, `FriendZone/Features/Hangouts/HangoutsView.swift`, `FriendZone/Features/Hangouts/HangoutsViewModel.swift`, `FriendZone/Features/Home/RootTabView.swift`, `FriendZone/Features/Onboarding/OnboardingFlowView.swift`, `FriendZone/Features/Tickets/TicketComponents.swift`. Untracked: `FriendZone/Features/Common/ApplePlaceSearch.swift` and sibling `../FriendZone-Frontend/`.
- `2026-03-12 23:45:51 CET` | `Coder-2` | `GPT-5 Codex` | `app-topology` | App entry is `FriendZone/FriendZoneApp.swift` -> `FriendZone/ContentView.swift`. Root routing depends on singleton `AppSessionStore.shared`: unauthenticated -> `AuthFlowView`, onboarding incomplete -> `OnboardingFlowView`, otherwise -> `RootTabView`. `RootTabView` combines Hangouts, Maps and Ambitions. `AppSessionStore` centralizes auth, profile, discovery, notifications and token persistence.
- `2026-03-12 23:45:51 CET` | `Coder-2` | `GPT-5 Codex` | `auth-analysis` | Current code still points login/register to `/api/auth/login/` and `/api/auth/register/` in both `FriendZone/Features/Auth/AuthFlowView.swift` debug metadata and `FriendZone/Core/AppSessionStore.swift` `AuthAPIService`. If backend custom auth is actually mounted at `/api/login/` and `/api/register/`, the iOS auth flow is broken today. This must be verified against backend before trusting any previous memory.
- `2026-03-12 23:45:51 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-analysis` | `FriendZone/Features/Hangouts/HangoutDetailView.swift` still seeds invented participants, chat messages and join requests before remote hydration. Fake names include `Sofia`, `Daniel`, `Mara`, `Luca`, `Mia Flores`, `Jonas Weber`. The screen later hydrates from backend, but initial UI can display fabricated social proof.
- `2026-03-12 23:45:51 CET` | `Coder-2` | `GPT-5 Codex` | `hangouts-loading-analysis` | `FriendZone/Features/Hangouts/HangoutsViewModel.swift` does not fetch remote data in `refresh()`: it clears `allHangouts`, resets request state and marks loading complete. Real network loading happens separately in `FriendZone/Features/Hangouts/HangoutsView.swift`. Responsibility is split between view and view model, which raises drift risk and transient empty-state risk.
- `2026-03-12 23:45:51 CET` | `Coder-2` | `GPT-5 Codex` | `architecture-risk` | UI modules are highly concentrated: `FriendZone/Features/Hangouts/HangoutsView.swift` has 7172 lines, `FriendZone/Core/AppSessionStore.swift` 2540, `FriendZone/Features/Hangouts/CreateHangoutView.swift` 3100, `FriendZone/Features/Hangouts/HangoutDetailView.swift` 1726, `FriendZone/Features/Creator/NativeCreatorSpaceHubView.swift` 2985. Compile-time, reviewability and regression risk are all elevated.
- `2026-03-12 23:45:51 CET` | `Coder-2` | `GPT-5 Codex` | `integration-notes` | Real backend integration exists across discovery, creator space, notifications, public profiles, hangout detail actions, and Apple Maps place search (`FriendZone/Features/Common/ApplePlaceSearch.swift`). The app is not just mock UI, but some surfaces still use local seeded data and optimistic local state.
- `2026-03-12 23:45:51 CET` | `Coder-2` | `GPT-5 Codex` | `test-analysis` | Test coverage is thin and contract-focused. `FriendZoneTests/FriendZoneTests.swift` mainly validates JSON decoding and `AppConfig.url(for:)`. There is no meaningful test coverage for auth flows, onboarding submission, hangout creation, session restoration, view model behavior, or navigation/state transitions.
- `2026-03-12 23:47:08 CET` | `Coder-2` | `GPT-5 Codex` | `build-verification` | Ran `xcodebuild` for scheme `FriendZone` with `CODE_SIGNING_ALLOWED=NO`. Result: `BUILD SUCCEEDED`. Current issues are therefore behavior/architecture risks, not an immediate compile break.
- `2026-03-12 23:52:25 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-fix` | Removed fabricated runtime social data from `FriendZone/Features/Hangouts/HangoutDetailView.swift`. The detail screen now initializes with host plus real `participantNames`, starts chat and join-requests empty until backend hydration, shows an empty chat state instead of fake messages, resets the requests panel when it disappears, and excludes the host from participant arrays coming from hangout mappers in list/map/detail flows. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 00:15:49 CET` | `Coder-2` | `GPT-5 Codex` | `map-rewrite` | Replaced the active maps experience with a new `FriendZoneMapExperience.swift` stack wired into `RootTabView`: custom MapKit pins for user/hangouts/events/offers, frosted chrome aligned with the app/web visual direction, selected-item card, locate-me FAB, and clustered hangout pins. Centering now prioritizes precise device location via `CLLocationManager`, falls back to geocoded city or stored viewport, and prevents late geocoder responses from overwriting real GPS coordinates. Added `NSLocationWhenInUseUsageDescription` to the app target and stabilized marker identity with string IDs for pins/clusters. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 00:21:29 CET` | `Coder-2` | `GPT-5 Codex` | `map-header-tuning` | Reduced the visual height of the new map header in `FriendZone/Features/Home/FriendZoneMapExperience.swift`: less top padding, tighter vertical spacing, smaller title/subtitle, smaller plan count badge, and slimmer chips. Goal was to preserve the same aesthetic while giving more map space above the fold. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 13:40:10 CET` | `Coder-2` | `GPT-5 Codex` | `map-header-positioning` | Lowered the vertical position of the new map chrome after user feedback. Added outer top padding to the header stack and reduced the internal safe-area top padding so the card sits lower on screen without growing in height. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 13:46:17 CET` | `Coder-2` | `GPT-5 Codex` | `map-header-positioning-v2` | Lowered the map header further by increasing the outer top offset of the chrome stack in `FriendZone/Features/Home/FriendZoneMapExperience.swift` from `14` to `30`, keeping the card height unchanged while moving it noticeably farther from the top edge. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 13:50:56 CET` | `Coder-2` | `GPT-5 Codex` | `map-header-positioning-v3` | Lowered the map header again by increasing the outer top offset of the chrome stack in `FriendZone/Features/Home/FriendZoneMapExperience.swift` from `30` to `48`. This keeps the same compact chrome styling while placing it substantially farther from the top edge. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 14:21:10 CET` | `Coder-2` | `GPT-5 Codex` | `map-home-location` | Adjusted the map to use the onboarding/home city as the default center instead of auto-recentering to device GPS. `RootTabView` now boots the map from `session.currentProfile.cityName`, keeps precise location centering only behind the locate button, and tracks whether the current viewport is using precise location for chrome state. Also nudged the map header slightly lower again by increasing the outer top offset from `48` to `56`. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 14:44:24 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-design-sync` | Reworked `FriendZone/Features/Hangouts/HangoutDetailView.swift` so the hangout detail now uses the real `HangoutCardView` from the Discover hangouts area as its hero card instead of a separate ticket design. Added a compact overview card for the long-form description and host/location context, flattened the page background back to the app background, and tightened secondary panels to the same surface/border language used across Discover. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 15:41:04 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-discover-frame-sync` | Corrected the hangout detail hero to use the exact same frame/layout as `/discover` hangouts by extracting the live Discover card into shared `FriendZone/Features/Hangouts/DiscoverHangoutCardView.swift` and mounting that component in both `HangoutsView` and `HangoutDetailView`. Also aligned detail horizontal padding from `20` to `16` so the hero width matches Discover spacing. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 15:51:23 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-nav-clearance` | Fixed overlap between the floating back/menu buttons and the hero card in `FriendZone/Features/Hangouts/HangoutDetailView.swift` by introducing a dedicated top content clearance (`topInset + 58`), moving the nav row slightly higher (`topInset + 8`), and aligning activity page horizontal padding to `16`. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 17:31:45 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-nav-clearance-tune` | Raised the hangout detail content slightly after the nav-clearance pass by reducing the dedicated top content clearance in `FriendZone/Features/Hangouts/HangoutDetailView.swift` from `topInset + 58` to `topInset + 42`, preserving separation from the floating back/menu controls while avoiding excessive empty space. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 17:35:48 CET` | `Coder-2` | `GPT-5 Codex` | `map-discover-hangout-consistency` | Aligned `RootTabView.loadMapHangouts()` with the base hangout set used by Discover by filtering map hangouts to `endAt > now` before building the displayed list and coordinate dictionary. This removes already-finished hangouts from the map and keeps the map’s hangout pool consistent with `HangoutsViewModel.futureHangouts`. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 17:41:45 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-nav-float-tune` | Moved the floating back/menu controls slightly upward in `FriendZone/Features/Hangouts/HangoutDetailView.swift` by reducing the nav row top inset from `topInset + 8` to `topInset + 4`, while leaving the content clearance unchanged so the hero card still stays clear of the controls. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 17:44:35 CET` | `Coder-2` | `GPT-5 Codex` | `map-header-dedup` | Removed the duplicated lower chip row from the map header in `FriendZone/Features/Home/FriendZoneMapExperience.swift`. The header now no longer repeats the city and plans count under the title/badge, and only keeps a temporary `Locating` chip when location lookup is active. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 17:47:20 CET` | `Coder-2` | `GPT-5 Codex` | `create-hangout-community-pill-removal` | Removed the top-right `Community` source pill from the hangout creation form in `FriendZone/Features/Hangouts/CreateHangoutView.swift` when `model.sourceType == .hangout`. The top bar now keeps the cancel action and centers the screen title via overlay, while still allowing non-hangout source pills to render on the trailing side. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 17:58:50 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-hero-card-consolidation` | Consolidated the duplicated top info surfaces in `FriendZone/Features/Hangouts/HangoutDetailView.swift` into a single hero card. The separate overview card was removed from `generalInfoPanel`, and the detail-specific content now lives inside the same Discover-style card via a footer slot added to `FriendZone/Features/Hangouts/DiscoverHangoutCardView.swift`. This keeps the top frame/style from Discover while eliminating repeated host/title/date information across two stacked cards. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 18:01:21 CET` | `Coder-2` | `GPT-5 Codex` | `map-header-hangout-badge-copy` | Updated the map header count badge in `FriendZone/Features/Home/FriendZoneMapExperience.swift` to use `HANGOUT` / `HANGOUTS` instead of `PLANS`, and reshaped the badge from a compact square into a horizontal capsule so the label reads cleanly with singular/plural counts. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 18:21:06 CET` | `Coder-2` | `GPT-5 Codex` | `discover-live-updates-unread-pulse` | Connected the `Live updates` indicator in `FriendZone/Features/Hangouts/HangoutsView.swift` to real unread notifications. The view now fetches unread count on initial load, on app foreground, and after dismissing the notifications hub; when unread notifications exist, the indicator switches from the normal primary dot to a red pulsing dot rendered with `TimelineView`. `NativeNotificationsHubView` now reports unread-count changes back to Discover after loading, marking one read, or marking all read, so the indicator returns to normal immediately when there are no pending notifications. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 18:41:51 CET` | `Coder-2` | `GPT-5 Codex` | `discover-hangout-emoji-pattern-card` | Redesigned the shared Discover hangout card in `FriendZone/Features/Hangouts/DiscoverHangoutCardView.swift` so the plain white background is replaced by a vibe-matched gradient plus a repeated emoji wallpaper pattern, inspired by the iPhone emoji wallpaper aesthetic. Added layered translucent inner panels for the main info, date block, and optional footer so typography stays legible against the richer background, while keeping the existing Discover ticket silhouette and allowing the detail hero card to stay visually aligned with Discover. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 18:46:47 CET` | `Coder-2` | `GPT-5 Codex` | `discover-hangout-emoji-pattern-neutralization` | Tuned the new Discover hangout wallpaper treatment in `FriendZone/Features/Hangouts/DiscoverHangoutCardView.swift` toward a more neutral/unisex look after the first pass felt too feminine. Replaced the softer pastel palettes with more mineral/stone-toned gradients, reduced the pattern’s chaos by lowering emoji opacity, shrinking rotation, tightening size variance, and using a more orderly grid rhythm, while keeping the emoji-wallpaper concept intact. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 18:56:07 CET` | `Coder-2` | `GPT-5 Codex` | `discover-hangout-iphone-wallpaper-match` | Adjusted the Discover hangout card wallpaper in `FriendZone/Features/Hangouts/DiscoverHangoutCardView.swift` to more closely match the provided iPhone emoji-wallpaper reference. Replaced the mineral gradient base with flatter vibe-matched base colors, introduced a more regular emoji grid via `GeometryReader` with alternating large/small emoji placements, removed the decorative rotation language, and kept the inner info/date/footer panels as translucent glass layers so the card feels closer to Apple’s emoji wallpaper composition rather than a decorative illustration background. Verified with `xcodebuild`: `BUILD SUCCEEDED`.
- `2026-03-13 19:02:30 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-data-coverage-analysis` | Audited `CreateHangoutDraft` / `CreateHangoutSubmission` against backend decoding, `HangoutItem` mapping, and `HangoutDetailView`. Conclusion: hangout detail currently does not cover all create-form data. Some fields are sent on create but not present in `HangoutFeedItem` (`languages`, `location_address`, `is_time_flexible`, `is_capacity_unlimited`, `visibility`, `invite_code`, `invite_code_hint`, `allow_waitlist`, `gender_preference`, `audience_tags`), some are dropped by `mapHangoutFeed` even when the response contains adjacent data (`coverImageUrl`, `sourceEventId`, `sourceOfferId`, exact coordinates/state), and the detail UI only varies by role for location/chat/requests while omitting most host-facing configuration and most guest-facing selection filters. No code change yet; this was an analysis pass to define the gap before redesigning the detail information architecture.
- `2026-03-13 19:21 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-role-aware-metadata` | Implemented the first real data-architecture pass for hangout detail so the screen can represent more of the create-form contract by role instead of only title/description/location/chat. Expanded `HangoutFeedItem` decoding in `FriendZone/Core/AppSessionStore.swift` with optional detail metadata (`locationAddress`, `isCapacityUnlimited`, `isTimeFlexible`, `languages`, `visibility`, `inviteCode`, `inviteCodeHint`, `allowWaitlist`, `genderPreference`, `audienceTags`), expanded `HangoutItem` in `FriendZone/Features/Hangouts/HangoutModels.swift` to retain those fields plus exact coordinates, cover image URL and source IDs, and updated all major hangout mappers (`HangoutsView`, `RootTabView`, `HangoutsViewModel`, create preview flow) so the metadata survives from create/feed/detail into the UI layer. `HangoutDetailView` now keeps a mergeable `DetailMetadata` state, adds `Plan details` and `Access` cards under the shared Discover-style hero, surfaces group size/timing/flexible-start/privacy/waitlist/languages/audience/gender filter visibility, shows invite code and hint only for hosts on invite-only hangouts, and uses metadata-aware city/location/address fallbacks for visitor vs accepted vs host states. Build verification did not expose Swift errors, but full `xcodebuild` is currently blocked in this environment by asset catalog compilation requiring unavailable simulator runtimes (`CoreSimulatorService` / `simdiskimaged`; `Assets.xcassets: error: No available simulator runtimes for platform iphonesimulator`).
- `2026-03-13 19:32 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-models-compile-fix` | Fixed a real Swift compile regression introduced while expanding `HangoutItem` metadata. The new stored properties in `FriendZone/Features/Hangouts/HangoutModels.swift` had been declared as `let` with default values and were also assigned inside the custom initializer, which triggers `immutable value 'self.<property>' may only be initialized once` for fields like `languages`, `locationAddress`, `visibility`, and related metadata. Removed those stored-property default initializers and kept the defaults only in the initializer signature. Also fixed two computed-property logic bugs by restoring explicit `return` statements in `spotsLeft` and `isFull`. Verified with `swiftc -typecheck FriendZone/Features/Hangouts/HangoutModels.swift`.
- `2026-03-13 19:41 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-structure-cleanup` | Simplified the top information architecture of `FriendZone/Features/Hangouts/HangoutDetailView.swift` after the first metadata pass still felt repetitive. The shared Discover card is now mounted without the extra footer so the hero stays visually exact to `/discover`. Removed the `Access` block entirely, replaced `Plan details` language with `Hangout details`, added a separate `About hangout` card for description text, moved structured fields (capacity, timing, visibility, waitlist, audience filter, languages, audience tags) into a single data-focused details card, and isolated host-only invite code/hint into a separate invite-only card. Also cleaned copy that still said `plans` in the public-profile stub to `hangouts`. Verification in this environment remains limited by Swift/Xcode cache and simulator-runtime issues; structural references were re-checked locally after the patch.
- `2026-03-13 19:46 CET` | `Coder-2` | `GPT-5 Codex` | `hangout-detail-compaction-pass` | Compactified the info page in `FriendZone/Features/Hangouts/HangoutDetailView.swift` so `People & Location` is visible again without losing the exact Discover hero. Reordered the stack so the shared hangout card is followed immediately by `People & Location`, then the structured `Hangout details`, then host-only invite details. Folded the description into the details card instead of spending a full extra card on `About hangout`, reduced section/card padding, tightened fact-tile typography and heights, slimmed chip pills, shortened the location map from `136` to `112`, and reduced participant pill/avatar sizes. Goal: more minimal density, less vertical waste, and better first-screen visibility of people/location metadata on smaller iPhones. Verification here remained limited to local reference checks because full Xcode build is still blocked by the simulator-runtime/asset-catalog environment issue.
- `2026-03-13 19:52 CET` | `Coder-2` | `GPT-5 Codex` | `discover-hangout-sheet-transition` | Replaced the Discover hangout push-navigation flow with a modal-from-bottom interaction that preserves the underlying feed state and scroll position. `FriendZone/Features/Hangouts/HangoutsView.swift` now uses `selectedHangoutDetail` plus `.sheet(item:)` with a large detent instead of `NavigationLink` for hangout cards, so tapping a card slides `HangoutDetailView` up from the bottom and dismissing returns the user to the exact Discover context underneath. `FriendZone/Features/Hangouts/HangoutDetailView.swift` now accepts an optional `onClose` callback and a configurable `closeButtonSystemName`, allowing Discover to render a downward chevron close control while map/hub flows keep the previous back-arrow behavior. Full Xcode build could not be re-verified here because the environment is still blocked by the `CoreSimulatorService` / asset-catalog runtime issue.
- `2026-03-13 20:01 CET` | `Coder-2` | `GPT-5 Codex` | `discover-detail-performance-pass` | Added a first targeted performance pass around the new Discover -> Hangout detail transition. In `FriendZone/Features/Hangouts/HangoutDetailView.swift`, the heavy location `Map` is now deferred behind a lightweight placeholder for ~180ms (`shouldRenderLocationMap`) so the first sheet-present frame does less work, the remote detail hydration is delayed ~120ms so it does not compete with the opening animation, and the offscreen activity page is no longer built until the user actually swipes up (`shouldRenderActivityPage`). In `FriendZone/Features/Hangouts/DiscoverHangoutCardView.swift`, the emoji wallpaper pattern was made sparser and cheaper by increasing the grid step, reducing large-emoji frequency, shrinking sizes, and softening rotation/opacity. Goal: smoother feed scrolling and less jank when opening a hangout from Discover while preserving the same visual direction.
- `2026-03-13 20:06 CET` | `coder-1` | `GPT-5 Codex` | `discover-hangout-card-cover-artwork-mode` | Updated the shared hangout card so any hangout with a cover image uses that photo as the card background in both Discover and Hangout Detail. Added support for local `coverImageData` and remote `coverImageURL`, normalized relative backend media URLs, and derived a darker matte palette from the image average color so panels, borders, chips, and date stub adapt to the artwork while staying readable. Emoji wallpaper remains as the fallback when no cover image exists.
